# =============================================================================
# LIVEKIT CONFIGURATION
# =============================================================================
# Get these from your LiveKit Cloud project:
# https://cloud.livekit.io/
#
# 1. Go to https://cloud.livekit.io/ and create a new project
# 2. Navigate to Settings → Keys
# 3. Create a new API key pair
# 4. Copy the values below

# LiveKit WebSocket URL (e.g., wss://your-project.livekit.cloud)
LIVEKIT_URL=wss://your-project.livekit.cloud

# LiveKit API Key
LIVEKIT_API_KEY=your-livekit-api-key

# LiveKit API Secret (keep this secret)
LIVEKIT_API_SECRET=your-livekit-api-secret

# =============================================================================
# OPENAI CONFIGURATION
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
#
# 1. Go to https://platform.openai.com/api-keys
# 2. Click "Create new secret key"
# 3. Copy the key below
# 4. Note: You'll need billing set up for API access
# 5. Ensure you have access to the Realtime API (gpt-4o-realtime-preview)

# OpenAI API Key (used for Realtime API: integrated STT, LLM, and TTS)
OPENAI_API_KEY=sk-your-openai-api-key

# =============================================================================
# NEXT.JS API CONFIGURATION
# =============================================================================
# The base URL of your deployed Next.js application
# This is used by the voice agent to call task CRUD endpoints
#
# For local development: http://localhost:3000
# For production: https://your-app.vercel.app

API_BASE_URL=http://localhost:3000

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================
# Health check server port (used by Railway, Render, etc.)
# Default: 8080
# This port is used for HTTP health checks at GET /health

HEALTH_CHECK_PORT=8080

# =============================================================================
# AGENT LIFECYCLE MANAGEMENT CONFIGURATION
# =============================================================================
# Configuration for log rate limiting to prevent Railway violations

# Log rate limit in logs per second (default: 400)
# Maximum log rate to prevent Railway log rate limit violations (500/sec limit)
LOG_RATE_LIMIT=400

# =============================================================================
# NOTES
# =============================================================================
# - Keep all API keys secure and never commit them to version control
# - For production deployment, set these variables in your hosting platform
#   (Railway, Render, etc.)
# - Make sure API_BASE_URL points to your deployed Next.js app in production
# - See SETUP.md for detailed setup instructions
